# Copy to .env and adjust. Real environment variables override these values.

# Base URL of the Drupal site running the Canvas Headless module,
# without a trailing slash. Only the app server uses it.
CANVAS_SITE_URL=https://canvas-env.ddev.site
